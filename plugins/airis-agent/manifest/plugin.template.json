{
  "name": "{{plugin_name}}",
  "version": "{{plugin_version}}",
  "description": "{{plugin_description}}",
  "author": {
    "name": "{{author_name}}"
  },
  "homepage": "{{homepage_url}}",
  "repository": "{{repository_url}}",
  "license": "{{license}}",
  "keywords": {{keywords_json}},
  "skills": "./skills/",
  "mcpServers": {
    "airis-agent": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/agiletec-inc/airis-agent",
        "airis-agent-mcp"
      ],
      "description": "Airis Agent MCP Server - Confidence Check, Repo Index, Deep Research"
    }
  }
}
